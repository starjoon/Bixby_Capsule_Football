action (GoToPlayer) {
  type (Constructor)
  description (go to player information)
  collect {
    input (playerTeam) {
      type (PlayerName)
      min (Required) max (One)
    }
    input (stats) {
      type (SearchStats)
      min (Optional) max (One)
    }
  }
  output (PlayerStats)
}
